import {chromium} from '@playwright/test';import {exercises} from '../public/engine.js';
const browser=await chromium.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true});const page=await browser.newPage({viewport:{width:1100,height:1100},serviceWorkers:'block'});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('http://localhost:4317');await page.locator('[data-page="training"]').click();for(const e of exercises){await page.locator(`[data-action="guide:${e.id}"]`).first().click();await page.waitForFunction(()=>document.querySelector('#human-stage')?.dataset.ready==='true');await page.locator('[data-phase="0.48"]').click();await page.waitForTimeout(100);await page.locator('#human-stage').screenshot({path:`../../work/pose-${e.id}.png`});await page.locator('dialog [data-action="close"]').first().click()}await browser.close();console.log({exercises:exercises.length,errors});
